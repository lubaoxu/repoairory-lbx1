#include "Main.h"
#include <stdio.h>
#include <chrono>
using namespace std::chrono;
using namespace std::chrono;
#define DAYS_PER_YEAR 365

int main() {
	int age; 
	age = 31;
	   
	printf("Your age is %d, and It is %d days. \n", age, nan);

	return 0;

}